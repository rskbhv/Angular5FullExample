import { OfferbannerDirective } from './offerbanner.directive';

describe('OfferbannerDirective', () => {
  it('should create an instance', () => {
    const directive = new OfferbannerDirective();
    expect(directive).toBeTruthy();
  });
});
